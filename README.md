# Hangul Note Staff

한글 음계 `도 레 미 파 솔 라 시 도`를 입력하면 A4 오선보 SVG/PNG/PDF와 실제 악보 교환 파일인 MusicXML로 변환하는 웹 서비스 MVP입니다.

## 실행

```powershell
cd C:\Users\eunho\Desktop\VibeCoding\HangulNoteStaff
py -3 -m http.server 5174
```

브라우저에서 `http://localhost:5174`를 엽니다.

## 사용법

- 한글 음계 입력: `도레미파솔라시도`, `도 레 미 파 | 솔 라 시 도`
- 쉼표 입력: `쉼`
- 음 길이 지정: `도/2`, `레/4`, `미/8`, `파/16`
- 높은 옥타브: `도'`, 낮은 옥타브: `도,`
- 옥타브 기호는 붙인 음에만 적용합니다. `도레미파솔라시도`의 마지막 `도`는 자동으로 올라가지 않습니다.
- 올림/내림: `도#`, `시b`
- PDF/TXT 업로드: 텍스트 레이어가 있는 PDF 또는 TXT에서 한글 음계를 추출합니다.
- 긴 악보: A4 한 쪽 분량을 넘으면 2쪽, 3쪽으로 자동 분할합니다.
- 화면의 `기호 사용법` 표와 사용 예시에서 입력 방법을 바로 확인할 수 있습니다.

## 산출물

- `index.html`, `styles.css`, `src/`: 실행 가능한 정적 웹앱
- `docs/stage-00-service-input.md` ~ `docs/stage-12-test-plan.md`: 프롬프트 단계별 산출물
- 내보내기: SVG, PNG, PDF, MusicXML

## 수정 표시

- 2026-05-16: 긴 계이름 입력 시 A4 용지 크기 미리보기로 줄바꿈/페이지 분할되도록 변경했습니다.
- 2026-05-16: PDF 다운로드를 추가했고, SVG/PNG는 여러 쪽일 때 페이지별 파일로 다운로드되도록 변경했습니다.
- 2026-05-16: 다운로드용 악보에서 좌측 상단 `Hangul Note Staff` 표기를 제거했습니다.
- 2026-05-16: 사이트 입력 패널에 악보 입력 기호 사용법 표를 추가했습니다.
- 2026-05-16: 기호 사용법 아래에 사용 예시 3개를 추가했습니다.
- 2026-05-16: 사용 예시를 라벨이 있는 카드 형태로 정리해 가독성을 개선했습니다.
- 2026-05-16: 기호표 스타일이 사용 예시 카드에 잘못 적용되던 문제를 수정했습니다.
- 2026-05-16: 사용 예시 아래에 추가했던 결과 예시는 가독성 판단 후 제거했습니다.
- 2026-05-17: `시` 다음 `도`를 자동으로 한 옥타브 올리던 동작을 제거하고, `'`/`,`가 붙은 음에만 옥타브 변경이 적용되도록 수정했습니다.

## 현재 제한

- PDF 입력은 텍스트 기반 PDF만 안정 지원합니다. 스캔 이미지 PDF의 OCR은 P1 범위로 분리했습니다.
- MusicXML은 C major, treble clef, 단선율 MVP 기준입니다.
- 서버 저장, 로그인, 관리자 화면은 MVP 출시 범위에서 제외했습니다.
